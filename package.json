{
  "name": "tsp-test",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "web app using ts-prelude",
  "scripts": {
    "start": "parcel",
    "build": "parcel build"
  },
  "devDependencies": {
    "parcel": "latest" 
  }
}
